export const API_BASE_URL = "http://localhost:5020/api";

export const API_ENDPOINTS = {
  auth: `${API_BASE_URL}/auth`,
  photos: `${API_BASE_URL}/photos`,
  uploads: "http://localhost:5020/uploads",
};

export const APP_CONFIG = {
  title: "RGUKT Photography Club",
  description: "Capture moments, share stories",
};
